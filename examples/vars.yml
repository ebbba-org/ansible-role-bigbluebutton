---

# Consider using ansible vault for the passwords you need to generate and fill below.
# More information on ansible vault at: https://docs.ansible.com/ansible/latest/user_guide/vault.html

# BBB
#bbb_state: present # For updating BigBlueButton with this role use 'latest'
#bbb_hostname: <your host fqdn> # Uncomment and fill if bbb fqdn is different than default {{ ansible_fqdn }}

# FREESWITCH
bbb_freeswitch_socket_password: "<secret>" # Generate and fill using `pwgen -s 16 1`

# MONITORING
#bbb_monitoring_htpasswd_user: <htpasswd_user> # Uncomment and fill, when using external monitoring
#bbb_monitoring_htpasswd: <htpasswd> # Uncomment and fill, when using external monitoring

# OPTIONAL
